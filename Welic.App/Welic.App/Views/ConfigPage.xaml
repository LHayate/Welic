<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ios="clr-namespace:UIKit;assembly=Xamarin.iOS"
             x:Class="Welic.App.Views.ConfigPage">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="lbAutenticado" Text="Usar Biometria" 
                   VerticalOptions="Center" 
                   HorizontalOptions="Center" />
            <!--<Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Label Text="Native Views Demo" FontAttributes="Bold" HorizontalOptions="Center" Grid.Column="0"/>
                <Switch IsToggled="{Binding IsOwned, Mode=TwoWay}" Grid.Column="1"
                        Toggled="Handle_Toggled" />
            </Grid>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>